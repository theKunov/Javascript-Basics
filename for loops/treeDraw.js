function star(input) {
    for(let i = 1 ; i <= 10 ; i++){
        let star = "";
        for(let n = 1 ; n <= i ; n++){
            star += "*" + ""
        }
        console.log(star)
    }
}